
const moves = ['up','down','left','right']

/**
 * Basic move algorithm - do some things and return a direction
 * @param {Object} board
 * @returns String 
 */
function basic(board) {
    return 'up';
}

module.exports = { basic } 